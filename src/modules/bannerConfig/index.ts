import * as bannerConfigController from './bannerConfig.controller';
import BannerConfig from './bannerConfig.model';
import * as bannerConfigService from './bannerConfig.service';
import * as bannerConfigValidator from './bannerConfig.validation';

export { BannerConfig, bannerConfigController, bannerConfigService, bannerConfigValidator };
